# CLAUDE.md — agent-trend-data

## このリポジトリの存在理由

このリポジトリは **ハブ**である。それ以外の何者でもない。

「データ収集システム(agent-trend-radar)」と「記事作成システム」という、
コンテキストを意図的に分離した2つのシステムの間で、データと記事原稿を
受け渡すための唯一の場所として存在する。ここでの生存理由が揺らぐような
変更(ロジックの追加、独自の処理の実装など)は原則として行わない。

## やって良いこと / やってはいけないこと

**やって良いこと**
- データ収集システムから送られてくるスナップショットを、決められたディレクトリ構造で保持する
- 記事作成システムが参照しやすいよう、最新データへの導線(`latest/`)を維持する
- 記事作成システムが書き込む「データへの要望」(`data-requests/`)を受け取り、蓄積する
- 記事作成システムが書いた記事原稿(`articles/`)を保管する
- スキーマ定義(`schema/SCHEMA.md`)を最新に保つ

**やってはいけないこと**
- 収集ロジック・記事生成ロジックをこのリポジトリに実装しない(それぞれ専用のリポジトリの責務)
- `snapshots/` 配下の過去データを上書き・削除しない(追記のみ。時系列の蓄積そのものが資産)
- SCSK九州の業務知見・機密情報を一切含めない(個人のOSS活動として完全に独立させる)

## ディレクトリ構成

```
agent-trend-data/
├── README.md
├── LICENSE                  # CC0 1.0 Universal(データは権利放棄)
├── schema/
│   └── SCHEMA.md             # 各フィールドの意味・型・バージョン定義
├── snapshots/
│   └── YYYY-MM-DD/
│       └── metrics.json      # その日の収集結果(追記のみ)
├── latest/
│   └── metrics.json          # 最新スナップショットのコピー(記事作成システムはまずここを見る)
├── manifest.json             # 利用可能なsnapshot日付の一覧
├── data-requests/
│   ├── pending/               # 記事作成システムからの要望(未対応)
│   └── done/                  # 対応済みの要望
└── articles/
    └── YYYY-MM/
        └── slug.md            # 記事原稿。front matterに based_on_snapshot, external_url 等を持つ
```

## 他リポジトリとの関係

- **データ収集システム(agent-trend-radar)**
  - GitHub Actionsが、このリポジトリ専用のFine-grained PAT(`Contents: Read and write`のみ)を使ってpushしてくる
  - このリポジトリ側からは何もアクションを起こさない(受け身の存在)
  - スキーマは常に `schema/SCHEMA.md` が正。収集システム側の実装が古い場合はスキーマ側ではなく収集システム側を直す

- **記事作成システム**
  - `latest/metrics.json` と過去の `snapshots/` を読み取り専用で参照する
  - 記事生成の過程で「このデータがあれば」と気づいたら、`data-requests/pending/` にテンプレートに沿ったMarkdownを追加する(実装はしない、要望を書くだけ)
  - 完成した記事原稿を `articles/` に追加する

## データの要望(data-requests)のフォーマット

`data-requests/pending/` に追加するファイルは、最低限以下を含む:

```markdown
---
type: 新規メトリクス | 収集対象追加 | 収集頻度変更 | データ品質問題
priority: 高 | 中 | 低
---

## 欲しいデータ
(具体的に)

## 根拠
(どの記事の、どの主張のために必要だったか)
```

このリポジトリ自体は要望をIssue化しない。要望の採否・Issue化は人間(オダワラ)が
別途データ収集システム側のIssueとして起票する。

## 運用方針

- 低コスト運用が前提。このリポジトリに自動化ロジックを増やさない
- 公開先(Zenn/note等)は未定。`articles/` は公開先が決まるまでの一次情報源として機能させる
- ライセンスはCC0。二次利用のハードルを最も下げる方針
